# REPORT — Tree of Thoughts 复现报告

> 📌 **2026-09-25 重大修正**：本报告的结论被**推翻并重写**。
> 首轮结论是"负结果：CoT 反超 ToT、方向与论文相反"——**那个 40% 是实现缺陷造成的假象**。
> 定位并修复缺陷后，ToT 在同一 15 题上达到 **100%**，方向与论文一致。
> 原首轮数据与完整排查过程保留在 **附录 A**（明确标注哪些结论已失效），修复记录在 **附录 B**。
>
> 一句话新结论：**方向复现，幅度大幅缩小；一个实现缺陷曾在中间制造出"方向反转"的假象。**
>
> 如果你只读一节，读 **[复盘：为什么这个缺陷没被发现](#复盘为什么这个缺陷没被发现)** ——
> 缺陷本身是普通的实现 bug，它在五类排查里活下来、还一度被写成结论，才是有信息量的地方。

---

## Claim

论文（Yao et al., NeurIPS 2023, Table 2）的断言：

- *"IO, CoT, and CoT-SC prompting methods perform badly on the task, achieving only 7.3%, 4.0%, and 9.0% success rates. In contrast, ToT with a breadth of b = 1 already achieves a success rate of 45%, while b = 5 achieves 74%."*
- 即：**Game of 24 上，同一个模型（GPT-4）用 CoT 只有 4%，用 ToT（BFS 搜索）有 74%——搜索带来了 70 个百分点的提升。**

---

## 论文报告 vs 我实测（修正后）

| 口径 | 论文（GPT-4-0613，100 题） | 我实测（glm-5.3-flash，15 题） |
|---|---|---|
| CoT 逐样本 | 4.0% | **85.07%**（1276/1500；三次跑 84.00/84.33/85.07） |
| CoT best-of-100（每题 100 次取至少一次成功） | 49% | **100%**（15/15） |
| CoT-SC（k=100） | 9.0% | 未做（见「我仍然不知道什么」） |
| ToT 逐候选（搜索出的每个最终轨迹） | 29.3%（官方日志复算） | **82.2%**（37/45 候选） |
| **ToT 每题（io 口径，与论文 74% 同口径）** | **74%**（b=5, e=3） | **100.0%**（15/15 题，b=3, e=1） |
| ToT top-1（value 排名第一，可部署、无需 oracle） | — | **100.0%**（15/15 题） |

## 结论

**方向与论文一致（ToT > CoT），但差距从 70 个百分点缩小到约 15 个百分点。**

| 口径 | 论文 | 本实验 | 差距 |
|---|---|---|---|
| 单次尝试 | 4% → 74%（+70 点） | 85.07% → 100%（+15 点） | **缩小到 1/5** |

**限定（必须一起说）**：n=15。ToT 15/15 = 100% 的 95% 置信下界约 **78%**（rule of three），
与 CoT 85.07% ± 0.9% 的置信区间**重叠** →
只能说 **"ToT 不劣于 CoT"**，**不能说"显著优于"**（任务书 §6.1 第 4 条的规矩）。

---

## 为什么幅度缩小（修正后的归因）

### 1. 基线被抬高了 —— 这是主因，且新数据支持它

论文的出发点是"自回归模型只能从左到右生成、无法回溯"，所以 GPT-4 单次只有 4%，
需要外部搜索兜底才到 74%。**2026 年的推理模型在单次调用里就做到了 85%**，
外部搜索的增量空间自然只剩 15 个点。

一手证据（保留自首轮，仍然有效）：把 CoT 提示喂给 glm-5.3-flash、抓取 `reasoning_content`
（隐藏推理流），一条样本里模型自己做了 5 次尝试、其中 2 次因"数字没用完"自我推翻、第 5 次成功。
**这正是 ToT 外部搜索干的活（lookahead + backtracking），现在在单次调用的推理流里完成了。**

> ⚠️ 修正：首轮据此推断"所以 ToT 会输"——**这个推论是错的**。
> 正确的推论是"**ToT 的优势会被压缩**"，而不是"ToT 会被反超"。
> 差距从 70 点缩到 15 点，正是"压缩"而不是"反转"。

### 2. value 打分是不可靠的启发式（事实保留，因果论断撤回）

`game24.py:90` 的三档权重 `{'impossible': 0.001, 'likely': 1, 'sure': 20}` 后面跟着
作者自己的注释 `# TODO: ad hoc` —— 没有理论依据，是随手定的。

> ⚠️ **首轮据此断言"价值模型的误剪是净损失"，并引用"900 题 CoT 99/100 对、ToT 却 0/3 全灭"
> 作为最干净的证据。这条论断已被推翻**：那个 0/3 全灭是**搜索跑偏**造成的
> （候选在用 1-shot 示例的数字 2/8/14），**不是 value 误剪**。
> 修复后同一题 ToT 变成 [1,0,1]，三候选中两个正确。
>
> 现在能说的是：**在当前模型下，value 判别够用**——修复后 ToT 在 15 题里
> **每题 value 排名第一的候选都是正确的（top-1 = 15/15）**。
> "value 到底靠心算还是模式匹配"仍是开放问题（见下）。

### 3. 搜索配置被弱化（b=3、e=1 vs 论文 b=5、e=3）

e=1 让打分没有平均（噪声直接进排序），b=3 让正确分支被误剪后替补更少。
这是"逐候选只有 82.2%"的部分原因，但**不是**与论文差距的主因（主因是第 1 条）。

### 4. 模型不是 GPT-4-0613

2023 年的 GPT-4 没有现代推理模型的内部回溯能力；这个 claim 高度依赖模型代际。

---

## 这个 claim 在我的规模下：**成立（方向一致）**，但幅度大幅缩小

- "ToT 的搜索机制"（propose→evaluate→select 的树搜索）**复现成功**：修复后 15/15 题走通完整链路。
- **"ToT 优于 CoT"这个结论迁移过来了**：单次尝试口径 100% vs 85.07%；best-of-k 口径打平（都 100%）。
- 但**幅度不迁移**：论文是 +70 个百分点，本实验是 +15 个百分点，且在 n=15 下统计上区分不开。
- 按任务书 §6.2 四条禁止自查：没有 cherry-picking（**所有失败运行都保留**，包括首轮 40%、复跑 26.7%、
  垃圾 run 1/崩溃 run 2 的复盘在 debug_log）；没有只报成功运行；没有说"论文有问题"；
  结论给出了机制解释而非一句"复现失败/成功"。

---

## 复盘：为什么这个缺陷没被发现

> 这一节是这次复现里最值得留下的部分。缺陷本身是普通的实现 bug；
> **它在五类排查里活下来、还一度被写成结论**，才是有信息量的地方。

五条原因，按"最隐蔽"排序：

### 1. 校准实验覆盖的是「评测链」，不是「搜索链」——最要命

首轮最强的那条证据是"把官方 gpt-4 日志喂进我们的 `test_output`，复现出论文的 3.47%/73.3%"。
但**那些日志是别人跑好的输出**——这个校准只走了**判定函数**那一段，**从来没有碰过 `propose`**。

所以"同一管道能复现论文数字"这句话，对**它实际验证的路径**（评测）成立，
对**出问题的那条路径**（搜索/提议）**根本没测过**。
它带来的是一种"我已经独立验证过了"的安全感，而验证的是另一个部位。

**教训**：做"同管道校准"时必须说清楚它覆盖了链路的哪几段。**校准的正确性不能外推到没走到的部分。**

### 2. 「实现」这一类只用 911 一道题验证就宣布排除

原报告写的是"ToT 全链路在 911 题验证 r=1（Answer 格式正确）；BFS 搜索逻辑未改动 → 排除"。
而示例回显是**随机的**（T=0.7），**单题成功完全不能排除一个按题发生的失效模式**。

**教训**：`n=1` 的验证只能支持"链路是通的"，不能支持"链路在每一题上都对"。证据的覆盖面必须匹配结论的强度。

### 3. 代码注释点出了病因，但规则治不住它

`bfs.py:37` 的注释原文就写着"会输出…**示例回显行**…需要过滤"。
**也就是说，团队知道这个风险存在**——但实现的规则只能看**形状**，
而回显行与真实提议的形状（数字开头 + 含 `left:`）**完全一致**。

**教训**：写了警告 ≠ 写了检查。**光看格式区分不出的东西，必须用语义约束（这里是数字池）来挡。**

### 4. 错误的结果看起来像一个合理的科学发现

"ToT 40% vs CoT 84%"不荒谬——而且手上立刻有一个漂亮的解释
（"现代推理模型把搜索内化了" + "value 模型只会猜不会算"）。
**它不像 bug，它像结论。**

这就是确认偏差最危险的地方：**解释越顺，越不会去翻原始数据**。
首轮的归因链条读起来非常自洽，而正因为自洽，没有人回头去看候选到底长什么样。

**教训**：当一个结果"恰好印证了一个漂亮的假说"时，反而要多做一次独立核查——
尤其是那些"支持假说"的关键证据（这里就是"900 题 0/3 全灭"）。

### 5. 聚合指标把 bug 藏起来了

报出来的都是汇总数（40.0%、28.2%）。要发现它，得**把候选原文打印出来**看：
```
[0] '\n\n8 / 2 = 4 (left: 4 8 14)\n14 - 8 = 6 (left: 4 6)\n'
[1] '\n\n14 - 8 = 6 (left: 2 6 8)\n\n'
[2] '\n\n2 * 8 = 16 (left: 8 14 16)\n\n'
```
而日志里有几千条样本，没人会去逐条读。**不做对账，就永远看不到。**

**教训**：聚合数字必须配一个"抽一条出来看着对不对"的动作。
这次是因为要解释"ToT 的分母为什么是 39"、跟着日志走了一遍才撞见——
**纯属偶然**。它应该是一个固定步骤，不是运气。

---

## 我仍然不知道什么

1. **value 模型的判断依据**：它是在做心算/树搜索，还是模式匹配？交换剩余数字顺序会不会改变打分？
   （没有做对照实验——修复后 top-1 全对说明它够用，但不知道它**怎么**做到的）
2. **ToT 与 CoT 的差距是否真实存在**：n=15、置信区间重叠，只能说"不劣于"。
   要区分开需要扩到 ~100 题（与论文对齐）。
3. **提议重试算不算对论文协议的偏离**：官方协议是"每节点 propose 一次、有什么用什么"；
   我们的重试最多 3 次（**只在输出无效时触发，取第一次有效结果，不是取并集**）。
   它的正当性来自"恢复官方假设（propose 总给出有效提议）"，但确实增加了调用量——
   这一点必须在任何引用本数字的场合显式声明。
4. **论文写 `sure/maybe/impossible`、代码写 `sure/likely/impossible`** 的差异影响多大。
5. 首轮"我想知道"里关于 deepseek-v4-pro 思考耗竭的那条仍开放（为什么 glm 系列没有）。

**（已删除的旧条目）** ~~"ToT 的边际贡献可能为零——解出的 6 题全是 CoT 本来就很强的题"~~：
该判断基于跑偏的搜索数据，已被推翻。修复后 ToT 解出全部 15 题，**包括 CoT 最弱的 913（66/100）、
914（60/100）** —— 这两题恰好是首轮 CoT 最弱的两题，现在 ToT 都拿到正确候选。

---

## 下一步怎么验（修正后）

1. **扩题量到 100 题**（与论文对齐）：这是唯一能解决"n=15 统计上区分不开"的办法。
   代价：CoT 100 题 ≈ 17 小时（逐样本口径 10000 次调用）。
2. **换弱模型**（如 glm-5.2）验证"搜索内化"假说：预测其 CoT 显著低于 85%。
   若弱模型上 ToT 的优势**重新拉开**，则第 1 条归因得到干净的机制证据。
   需要先给 `run.py` 的 `--backend` 加模型名。
3. **恢复论文配置 b=5、e=3**：io/top-1 已经 100%，只能看逐候选能否从 82.2% 再提升。

---

## 逐题明细（修正后：glm-5.3-flash，T=0.7，15 题）

**同一天、同一把 key 的同期对照**。CoT = 每题 100 样本的逐样本正确数；ToT = 3 个最终候选的 r。

| 题目 | CoT 对/100 | ToT 候选 r | ToT 解出? | 题目 | CoT 对/100 | ToT 候选 r | ToT 解出? |
|---|---|---|---|---|---|---|---|
| 900 | 95 | [1,0,1] | ✅ | 908 | 87 | [1,1,1] | ✅ |
| 901 | 68 | [1,1,1] | ✅ | 909 | 88 | [1,1,1] | ✅ |
| 902 | 94 | [1,1,1] | ✅ | 910 | 92 | [1,0,1] | ✅ |
| 903 | 87 | [1,1,1] | ✅ | 911 | 91 | [1,0,0] | ✅ |
| 904 | 96 | [1,1,1] | ✅ | 912 | 88 | [1,1,1] | ✅ |
| 905 | 84 | [1,0,0] | ✅ | 913 | **66** | [1,1,1] | ✅ |
| 906 | 92 | [1,1,0] | ✅ | 914 | **60** | [1,1,0] | ✅ |
| 907 | 88 | [1,1,1] | ✅ | | | | |

**值得注意**：CoT 最弱的两题（913 = 66/100、914 = 60/100）**都是 ToT 解出的**。
首轮报告曾据此得出"ToT 边际贡献为零"，那个结论建立在跑偏的数据上。

## 数据与存档

| 用途 | 文件 |
|---|---|
| CoT 同期重跑（85.07%） | `logs/game24/glm-5.3-flash_0.7_naive_cot_sample_100_start900_end915.json` |
| CoT 首轮分片（84.00%） | `..._start{900,905,910}_end*.json` |
| CoT 复跑（84.33%，已被同期重跑覆盖） | 存档 `logs/archive/..._final_20260921.json` |
| **ToT 修复后（100%）** | `logs/game24/glm-5.3-flash_0.7_propose1_value1_greedy3_start900_end915.json` |
| ToT 首轮 40% / 复跑 26.7%（已失效，留证） | `logs/archive/..._20260922.json` 共 4 份 |
| 官方 gpt-4 校准日志 | `logs/game24/gpt-4_*.json` |
| 门禁测试 | `~/gate_tot_valid.py`（`PASS`：45/45 候选起步合法） |
| 独立复核脚本 | `~/verify_numbers.py`、`~/final_compare_fixed.py` |

---

# 附录 A：被修正的过程（首轮结论 + 五类排查，**已失效**）

> ⚠️ 附录 A 整节记录的是**首轮的真实结论与排查过程**，其中主结论已失效。
> 保留它是因为"为什么当时会得出错误结论"本身就是这次复现最值钱的部分。
> **失效的论断在下面用 ~~删除线~~ 标出。**

## A.1 首轮结论（已失效）

~~**结论方向与论文完全相反：CoT 反超 ToT 44-60 个百分点（视口径）。**~~

首轮实测：CoT 逐样本 84.0%（1260/1500）／best-of-100 100%（15/15）／
ToT 每题 40.0%（6/15，b=3/e=1）／ToT 逐候选 28.2%（11/39）。

~~方差检查：CoT 逐样本随机误差 ≈ sqrt(0.84×0.16/1500) ≈ ±1%；两组差异 44 个百分点，
远超噪声，不存在"当前预算下不可判定"的问题，反转是真实的。~~

（复跑 26.7% 那次见附录 A.4。）

## A.2 五类排查（2026-09-21）——**漏洞在「实现」那一行**

| 类别 | 当时的证据 | 当时的结论 | 事后复核 |
|---|---|---|---|
| 数据 | 官方 24.csv 原题；题目串与官方日志逐题一致 | 排除 | ✅ 成立 |
| **实现** | **只用 911 一道题**验证 r=1（Answer 格式正确）；BFS 搜索逻辑未改动 | **排除** | ❌ **不成立**：实际有 6/15 题的搜索从第 1 步就在错误的数字上。**一条题的验证覆盖不了这个结论** |
| 超参 | b=3/e=1 vs b=5/e=3，只会削弱 ToT | 部分相关 | ✅ 成立 |
| 评测协议 | `test_output` 一行未动，且被校准独立验证 | 排除 | ✅ 成立（这次也没动） |
| 计算预算 | 约 2000 次调用，月额度 11%；CoT 95% CI ±1% | 排除 | ✅ 成立 |

## A.3 同管道校准（这一步的方法本身有效，且现在更强）

把官方仓库自带的 GPT-4 日志在同样 15 题上用同一个 `test_output` 复算：

- 官方 CoT 日志：**3.47%**（52/1500），论文 4.0%
- 官方 ToT 日志（b=5/e=3）：**73.3%**（11/15），论文 74%

→ 说明评测管道能把论文数字复现出来，数据/评测/实现**在"评测链路"这个意义上**没问题。
**但当时把"评测链路没问题"错当成了"整个实现没问题"**——搜索侧的缺陷不在 `test_output` 覆盖范围内。

> 事后补充：现在还额外校验了论文 CoT 家族的另两行——CoT-SC(k=100) **9.0%**、CoT(best of 100) **49.0%**，
> 与论文 Table 2 一字不差（脚本 `~/compute_cot_sc.py`）。校准链因此更完整。

## A.4 首轮的两条归因（一条保留、一条撤回）

1. **模型代差：搜索被内化进了推理模型** —— ✅ **保留**（见正文归因第 1 条），
   但当时由此推出"ToT 会输"是错的；正确推论只是"优势被压缩"。
2. ~~**价值模型的误剪是净损失**，证据"900 题 CoT 99/100 对、ToT 却 0/3 全灭"~~
   —— ❌ **撤回**。那个 0/3 是搜索跑偏（候选用了示例数字 2/8/14），不是 value 误剪。
3. 搜索配置弱化（b=3/e=1）—— ✅ 保留。
4. 模型不是 GPT-4-0613 —— ✅ 保留。

## A.5 首轮"我仍然不知道什么"第 4 条（已失效）

~~ToT 解出的 6 题全是 CoT 本来就很强的题，没有任何一题是"CoT 不会而 ToT 会"的，
暗示 ToT 的边际贡献可能为零。~~ → 已被推翻，见正文。

## A.6 首轮逐题明细（已失效，留证）

| 题目 | CoT 对/100 | ToT 解出? | ToT 正确候选 | 题目 | CoT 对/100 | ToT 解出? |
|---|---|---|---|---|---|---|
| 900 | 99 | ❌ | 0/3 | 908 | 88 | ✅ |
| 901 | 74 | ❌ | 0/1 | 909 | 85 | ❌ |
| 902 | 91 | ✅ | 3/3 | 910 | 90 | ❌ |
| 903 | 83 | ❌ | 0/3 | 911 | 87 | ✅ |
| 904 | 96 | ❌ | 0/3 | 912 | 85 | ✅ |
| 905 | 80 | ✅ | 1/3 | 913 | 58 | ❌ |
| 906 | 94 | ❌ | 0/3 | 914 | 61 | ❌ |
| 907 | 89 | ✅ | 2/3 | | | |

（ToT 首轮的 4 次无效被标为 ❌ 的题里，**6 题是因为搜索跑偏**：900、901、903、909、910、913。）

## A.7 首轮的两处次要发现（仍然有效）

- **题号映射**：`task.get_input(i)` 返回官方 `24.csv` 的 **Rank (i+1)**，所以"15 题 900-914"
  实为 **Rank 901-915**。溯源：本组适配 commit `cd8b9ef` 对 `game24.py` 的 diff 为**空**，属官方行为
  （也解释了论文写 "indices 901-1000" 而命令写 `--task_start_index 900`）。
  所有 CoT/ToT/校准日志用同一套约定，比较的是同一批题，**对结论无影响**。
- **一次未记录的 ToT 重跑**：`..._greedy3_start900_end915.json`（9/21）是另一次运行，
  结果 4/15 = 26.7%、逐候选 6/41。按任务书 §6.2"不许只报成功的那次"，两次都留证。

---

# 附录 B：缺陷定位与修复记录（2026-09-25）

## B.1 缺陷：搜索在约 1/3 题目上从第 1 步就跑偏

`get_proposals` 的过滤器把模型**回显的 propose prompt 1-shot 示例**当成合法提议放行。
示例输入是 `2 8 8 14`，而回显行的形状（数字开头 + 含 `left:`）与真实提议**完全一致**，
过滤器无法区分。后果：6/15 题的搜索从第 1 步就在错误的数字上进行。

**证据（修复前）**：题 900 的三个最终候选原文——
```
[0] '\n\n8 / 2 = 4 (left: 4 8 14)\n14 - 8 = 6 (left: 4 6)\n'
[1] '\n\n14 - 8 = 6 (left: 2 6 8)\n\n'
[2] '\n\n2 * 8 = 16 (left: 8 14 16)\n\n'
```
题目是 `4 5 6 10`，三个候选用的是示例的 2/8/14。

**代码注释其实预警过**（`bfs.py:37` 写了"示例回显行…需要过滤"），
但规则 `_[0].isdigit() and 'left:' in _` 治不住它——**注释点出了病因，实现没治住**。

另一种失效模式是**空轨迹**：模型输出被过滤器全部滤掉 → 兜底 `proposals = ['']`
→ 搜索空转（901、909、910、914 属于这类）。

## B.2 修复（不改评测红线）

`repro/tot/src/tot/methods/bfs.py`（+42/−8）：

1. **数字池校验**：步骤行的两个操作数必须在**当前剩余数字池**里（从轨迹最后一行
   `left: ...` 推出；非 game24 格式返回 None 表示不做校验）→ 回显行自然被拒。
2. **提议重试**：过滤后为空时重试提议（最多 3 次），不再直接兜底成空候选。

**`test_output`（评测红线）一行未动。**

## B.3 验证（三级，逐级加强）

| 级别 | 做法 | 结果 |
|---|---|---|
| 1. 离线单测 | `repro/tot/tests/test_proposal_filter.py`：用日志里**真实的 8 行回显**做断言 | 全 PASS（并附"旧规则会放行全部 8 行"的回归对照） |
| 2. 门禁测试 | `~/gate_tot_valid.py`：判据 = 轨迹**第 1 步**是否用题目数字起步。**拿官方 gpt-4 日志作对照组** | 官方日志 75/75 全合法（证明判据正确）；glm 首轮 6/15 失效 |
| 3. 实跑 | 全量 15 题重跑（同参数、同 key、同日） | 门禁 **PASS**（45/45 起步合法）；io 15/15、top-1 15/15 |

## B.4 同期 CoT 对照（排除"模型变强"这个解释）

同一天、同一把 key、同一模型重跑 CoT 基线：

| 口径 | 首轮 9/20 | 复跑 9/21 | **同期重跑 9/25** |
|---|---|---|---|
| CoT 逐样本 | 84.00% | 84.33% | **85.07%** |
| CoT best-of-100 | 100% | 100% | **100%** |

三次全在 ±1% 内一致 → 模型服务稳定。**ToT 的 100% 不是"今天模型变强"造成的。**

## B.5 三处值得单独记的教训

1. **五类排查里「实现」只用 911 一道题验证就宣布排除** —— 证据覆盖面不匹配结论强度。
   教训：验证链路的覆盖面必须匹配结论的强度。
2. **"能失败的检查"≠"判据正确的检查"**：门禁判据先生错两版——v1 加了"left 标注必须精确"
   （连**论文自己的 gpt-4 数据**都误判为坏），v2 要求"所有步都合法"（把"正常解不出"当成了失效）。
   第三版只看第 1 步，并用**官方日志作对照**才验证判据本身正确。详见 `agent_log.md`。
3. **换模型绕过 ≠ 修好**：附录 B.1 的空轨迹根因（思考 token 耗尽 max_tokens）在首轮的
   `debug_log` 里就记过，当时靠换模型绕开、没根治，这次在新模型上换个题目复发。详见 `debug_log.md`。
