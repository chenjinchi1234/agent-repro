# REPORT — Tree of Thoughts 复现报告（负结果）

> 任务书 6.3 模板逐项填写。每个数字的来源都可解释，逐题明细在文末。

## Claim

论文（Yao et al., NeurIPS 2023, Table 2）的断言：

- *"IO, CoT, and CoT-SC prompting methods perform badly on the task, achieving only 7.3%, 4.0%, and 9.0% success rates. In contrast, ToT with a breadth of b = 1 already achieves a success rate of 45%, while b = 5 achieves 74%."*
- 即：**Game of 24 上，同一个模型（GPT-4）用 CoT 只有 4%，用 ToT（BFS 搜索）有 74%——搜索带来了 70 个百分点的提升。**

## 论文报告 vs 我实测

| 口径 | 论文（GPT-4-0613，100 题） | 我实测（glm-5.3-flash，15 题 900-914） |
|---|---|---|
| CoT 逐样本 | 4.0% | **84.0%**（1260/1500 样本） |
| CoT best-of-100（每题 100 次取至少一次成功） | 49% | **100%**（15/15 题） |
| ToT 逐候选（搜索出的每个最终轨迹） | 未报告 | 28.2%（11/39 候选） |
| ToT 每题（io 口径，与论文 74% 同口径） | 74%（b=5, e=3） | **40.0%**（6/15 题，b=3, e=1） |

**结论方向与论文完全相反：CoT 反超 ToT 44-60 个百分点（视口径）。**

方差检查（任务书 6.4）——完整计算见下方「任务书 6.1 处理流程完整记录」第 4 步；结论：CoT 逐样本的随机误差 ≈ sqrt(0.84×0.16/1500) ≈ ±1%；两组差异 44 个百分点，远超噪声，**不存在"当前预算下不可判定"的问题，反转是真实的**（在本文配置下）。

## 任务书 6.1 处理流程完整记录（2026-09-21 补）

### 第 1 步：先假设是自己的问题，五类排查（每类给证据）

| 类别 | 证据 | 结论 |
|---|---|---|
| 数据 | 官方 24.csv 原题（900-914）；题目串与官方日志逐题一致 | 排除 |
| 实现 | ToT 全链路在 911 题验证 r=1（Answer 格式正确）；BFS 搜索逻辑未改动 | 排除 |
| 超参 | 差异已声明（b=3/e=1 vs b=5/e=3），方向只会削弱 ToT | 部分相关：解释 ToT 的 40%，不解释 baseline 从 4% 到 84% |
| 评测协议 | test_output 一行未动；且被第 2 步校准独立验证 | 排除 |
| 计算预算 | 全程约 2000 次调用，本月额度仅用 11%；CoT 逐样本 95% 置信区间 ±1%，预算未卡住任何数字 | 排除 |

### 第 2 步：看 baseline —— 用官方日志做同管道校准（本流程最强证据）

把官方仓库自带的 GPT-4 日志在我们同样的 15 题上用同一个 test_output 复算：

- 官方 CoT 日志（gpt-4_0.7_naive_cot_sample_100_start900_end1000.json）：**3.5%**（52/1500），论文 4.0%
- 官方 ToT 日志（gpt-4_0.7_propose1_value3_greedy5_start900_end1000.json，配置正是论文的 b=5/e=3）：**73.3%**（11/15），论文 74%

baseline 对得上、实验组也对得上（差异在 15 题抽样噪声内）。同一管道喂 gpt-4 的输出就复现论文数字，因此数据、实现、评测协议全部排除嫌疑，我们的数字与论文的差异全部归因于模型替换（外加声明过的搜索配置弱化）。

### 第 3 步：规模问题检查

样本维度与论文对齐（每题 100 个 CoT 样本）；题目维度 15 vs 100。反转差距 44-60 个百分点且逐题方向一致（CoT best-of-100 对 15 题全对、ToT 只对 6 题），不依赖大数定律——15 题足以支撑方向性结论。

### 第 4 步：方差检查（同口径：每题至少一次成功，n=15）

- 若 ToT 真实每题成功率 ≥ 60%，观察到 ≤6/15 的概率 ≈ 2%；
- 若 CoT 真实每题成功率 ≤ 80%，观察到 15/15 的概率 ≈ 3.5%。

两组差异远超任何一方置信区间的宽度：**不写「当前预算下不可判定」，反转在 ~95% 置信下成立**（在我们声明的配置下）。

## 我排除的（每类都给了证据）

- **数据**：用的是官方仓库 `24.csv` 原题（第 900-914 题），题目本身与官方日志 `logs/game24/gpt-4_..._start900_end1000.json` 完全一致；数字无改动。
- **实现**：ToT 的机制完整跑通——propose→value→贪心选 b→24 特判→Answer 行→判定，全链路在 911 题上验证 r=1（日志：`Answer: ((1 + 8) + 2) + 13 = 24`，且 911 修复过滤后重跑成功）；官方 gpt-4 日志里正确轨迹的格式与我们生成的格式一致。BFS 代码的搜索逻辑未改动（只加了 propose 输出的过滤与空候选兜底，均属"让官方假设在新模型上重新成立"，详见 paper_notes 实验设计节）。
- **超参**：与论文的差异是**声明过的、有方向的**：b=5→3、e=3→1、模型 GPT-4→glm-5.3-flash。b、e 变小只会**削弱** ToT，不能解释"CoT 从 4% 变成 84%"——后者是 baseline 本身的移动。
- **评测**：判定函数 `test_output`（game24.py:44-55）**一行未动**：最后一行 Answer 格式 + 数字多重集合 + sympy 验算，与官方一致。评测协议不是数字反转的来源。

## 我认为的原因（证据链）

1. **模型代差：搜索被内化进了推理模型。** ToT 论文的出发点——"自回归模型只能从左到右生成，无法回溯"——在 2026 年的推理模型上已经改变。实测证据：把 CoT 提示喂给 glm-5.3-flash，抓取其 `reasoning_content`（隐藏推理流），一条样本里模型自己做了 5 次尝试、其中 2 次因"数字没用完"自我推翻、第 5 次成功。**这正是 ToT 外部搜索干的活（lookahead + backtracking），现在在单次调用的推理流里完成了。** 基线从 4% 挪到 84%，主要动力在这里。
2. **价值模型的误剪是净损失。** 当基线已经 84%，外部搜索的收益上限只有 16 个百分点；而每步裁剪都依赖"只会猜、不会算"的 value 模型（三档打分，代码自注 "TODO: ad hoc"）。最干净的证据：**900 题 CoT 99/100 做对，ToT 却 0/3 全灭**——搜索在模型本可轻松解决的题上，把正确分支剪掉了。垃圾行 "Possible next steps:" 被 value 判 sure=20 进入 beam 是同类证据。
3. **搜索配置被弱化（b=3、e=1 vs 论文 b=5、e=3）。** e=1 让打分噪声更大（没有平均），b=3 让正确分支被误剪后无替补。这一条是"我们的 ToT 只有 40% 而论文有 74%"的原因之一，但**不是**反转的原因——即使 ToT 恢复到论文配置，它最多接近、无法超过自带搜索的基线（原因 1 的收益上限论证）。
4. **模型不是 GPT-4-0613。** 2023 年的 GPT-4 没有现代推理模型的内部回溯能力，外部搜索对它价值极大；这个 claim 高度依赖模型代际。

## 这个 claim 在我的规模下：不成立（方向反转），且机制成立

- "ToT 的搜索机制"（propose→evaluate→select 的树搜索）**复现成功**，40% 的题走通了与论文同构的完整链路。
- "ToT 优于 CoT"这个结论**不迁移**：在自带推理回溯的强模型 + 弱化搜索配置下，外部搜索只剩误剪副作用。
- 按照任务书 6.2 的四条禁止自查：没有 cherry-picking（所有失败运行保留在 `repro/tot/logs/`，包括垃圾 run 1 和崩溃 run 2 的复盘在 debug_log）；没有只报成功运行；没有说"论文有问题"——论文的数字在其时代和模型下是成立的；结论给出了解释而非一句"复现失败"。

## 下一步怎么验（具体动作）

1. **换弱模型**：在 glm-5.2 或 kimi-k3 上跑同一 15 题（小样本 20 个/题先试），预测其 CoT 显著低于 84%——若 ToT 反超，则完整复现论文的"搜索收益"结论。
2. **恢复论文配置**：glm-5.3-flash 上跑 b=5、e=3（约 3 倍调用量，配额充足：本月仅用 11%），看 ToT 能否逼近 84% 基线——预计不能超过，但能量化"搜索收益上限"。
3. **价值模型能力测试**：构造 50 个"人类可判定是否可达 24"的中间状态，测 value 模型的判准率——把"误剪"从轶事变成数字。

## 我仍然不知道什么

1. value 模型的判断依据是什么：它是在做心算/树搜索，还是模式匹配？交换剩余数字顺序会不会改变打分？（没有做对照实验）
2. 论文 "sure/maybe/impossible" 与代码 sure/likely/impossible 的差异对结果影响多大——权重 {0.001, 1, 20} 是官方自注 ad hoc 的，改了会怎样？
3. deepseek-v4-pro 跑 8 示例 few-shot propose 时"思考耗竭"（finish_reason=length）的内部机制——为什么 glm 系列没有这个问题，是否与训练时对长 prompt 的处理有关。
4. ToT 在 913/914 两道难题（CoT 仅 58%/61%）上也失败了——搜索到底有没有帮上任何一题？逐题看，ToT 解出的 6 题全是 CoT 本来就很强的题，**没有任何一题是"CoT 不会而 ToT 会"的**。这暗示在我们的配置下 ToT 的边际贡献可能为零，但样本太小不能下结论。

## 逐题明细（glm-5.3-flash，temperature 0.7，15 题）

| 题目 | CoT 对/100 | CoT best-of-100 | ToT 解出? | ToT 正确候选 | 题目 | CoT 对/100 | ToT 解出? |
|---|---|---|---|---|---|---|---|
| 900 | 99 | ✅ | ❌ | 0/3 | 908 | 88 | ✅ |
| 901 | 74 | ✅ | ❌ | 0/1 | 909 | 85 | ❌ |
| 902 | 91 | ✅ | ✅ | 3/3 | 910 | 90 | ❌ |
| 903 | 83 | ✅ | ❌ | 0/3 | 911 | 87 | ✅ |
| 904 | 96 | ✅ | ❌ | 0/3 | 912 | 85 | ✅ |
| 905 | 80 | ✅ | ✅ | 1/3 | 913 | 58 | ❌ |
| 906 | 94 | ✅ | ❌ | 0/3 | 914 | 61 | ❌ |
| 907 | 89 | ✅ | ✅ | 2/3 | | | |

原始数据：`repro/tot/logs/game24/glm-5.3-flash_0.7_naive_cot_sample_100_start{900,905,910}*.json`（CoT）与 `glm-5.3-flash_0.7_propose1_value1_greedy3_start{900,905,910}*.json`（ToT）。汇总脚本：`combine.py` / `final_compare.py`（在 WSL home，未进仓库——统计脚本允许，评测逻辑不允许）。
